import React from "react";

const Loading = () => {
  return (
    <div className="h-5 animate-bounce transition-all duration-300 text-md">
      Loading...
    </div>
  );
};

export default Loading;
